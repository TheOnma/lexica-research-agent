# Container builds hit flaky networking (TLS socket drops mid-download) on
# some hosts — retry aggressively instead of failing the docker build.
fetch-retries=10
fetch-retry-mintimeout=1000
fetch-retry-maxtimeout=120000
network-concurrency=4
